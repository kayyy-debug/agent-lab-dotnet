@using SocOps.Models

<div class="game-screen">

    <header class="game-header">
        <button @onclick="OnReset" class="btn-back">← Back</button>
        <h1 class="game-title">Soc Ops</h1>
        <div class="header-spacer"></div>
    </header>

    <p class="game-hint">Tap a square when you find someone who matches it.</p>

    @if (HasBingo)
    {
        <div class="bingo-banner">🎉 BINGO! You completed a line!</div>
    }

    <div class="game-board-wrap">
        <BingoBoard
            Board="@Board"
            WinningSquareIds="@WinningSquareIds"
            OnSquareClick="@OnSquareClick" />
    </div>

</div>

@code {
    [Parameter]
    public List<BingoSquareData> Board { get; set; } = new();

    [Parameter]
    public HashSet<int> WinningSquareIds { get; set; } = new();

    [Parameter]
    public bool HasBingo { get; set; }

    [Parameter]
    public EventCallback<int> OnSquareClick { get; set; }

    [Parameter]
    public EventCallback OnReset { get; set; }
}
